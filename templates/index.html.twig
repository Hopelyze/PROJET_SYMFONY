<!DOCTYPE html>
<html lang="fr">
<head class="site-header">
    <link rel="stylesheet" href="{{ asset('css/menu.css') }}">
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{% block title %}Mon Site{% endblock %}</title>
</head>

<body>

    {# Inclusion du menu #}
    {% include 'layout/_menu.html.twig' %}

    {# Contenu principal #}
    <main>
        {% block content %}
            <h1>Bienvenue, {{ moi }} !</h1>
            <p>Vous êtes connecté(e) en tant que {{ moi }}.</p>

             <section id="products">
                <h2>Nos Fleurs</h2>
                <div class="product-list">
                    {% for fleur in fleurs %}
                        <div class="product-card">
                            <h3>{{ fleur.libelle }}</h3>
                            <p>Prix : {{ fleur.prix }} €</p>
                           <p>Stock : {{ fleur.quantiteStock }}</p>
                          </div>
                    {% else %}
                        <p>Aucun produit disponible.</p>
                    {% endfor %}
                </div>
            </section>
        {% endblock %}
        
    </main>

</body>
<footer>
    {# Inclusion du footer ici #}
    {% include 'layout/_footer.html.twig' %}
</footer>
</html>